import { FipePercentagePipe } from './fipe-percentage.pipe';

describe('FipePercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new FipePercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
