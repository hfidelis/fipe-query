import { StatusPercentagePipe } from './status-percentage.pipe';

describe('StatusPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new StatusPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
