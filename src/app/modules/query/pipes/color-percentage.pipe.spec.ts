import { ColorPercentagePipe } from './color-percentage.pipe';

describe('ColorPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
